import {DarkTheme} from 'react-native-paper';

const theme = {
    ...DarkTheme,
    colors: {
      ...DarkTheme.colors,
      primary: '#2d3436',
      accent: '#1C1C1C',
      background : '#636e72'
    }
  };


export default theme;